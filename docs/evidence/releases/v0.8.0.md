# CSSwitch v0.8.0 发布证据

复核时间：2026-07-19（Asia/Shanghai）。本文件分别记录 release source、自动门禁、最终 DMG 与公开分发；自动测试和只读挂载不能外推为所有真实 provider、账号或 Science 环境均已通过。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| release source / 发布时 `origin/main` | `4b163d50178791e7fbf9e085eb06fc2260baed4e` |
| annotated tag object | `5ceafab7bab61dce8feeba5343e6fca4a06c4414`；unsigned |
| peeled `v0.8.0^{}` | `4b163d50178791e7fbf9e085eb06fc2260baed4e` |
| 版本 / 架构 | `v0.8.0` / macOS Apple Silicon |
| GitHub Release | `CSSwitch v0.8.0`；已公开，非 draft、非 prerelease |
| 发布时间 | `2026-07-19T07:10:43Z` |
| 页面 | `https://github.com/SuperJJ007/CSSwitch/releases/tag/v0.8.0` |

tag 固定在包含新 UI、新 README、演示 GIF、CHANGELOG 与 v4 升降级说明的 exact source commit。发布证据在公开回读后补写，因此后续 `main` 可领先 tag；release identity 以 peeled tag 为准。

## 自动门禁与构建

在 release source 上运行：

```text
bash test/run_all.sh --require-release-ready
```

命令退出 0，offline、loopback、scripts、rust、frontend 五层通过并汇总为 `release-ready green: YES`。Desktop 为 350 passed / 2 explicit ignored，Gateway 为 232 lib + 1 CLI passed；ignored 和未执行层不计作自动通过。`npm ci` 通过且报告 0 vulnerabilities，`npm run tauri build` 从同一提交产出 arm64 app 与 DMG。

## 最终附件身份

| 项目 | 值 |
|---|---|
| 文件 | `CSSwitch_0.8.0_aarch64.dmg` |
| 大小 | 23,828,770 bytes |
| SHA-256 | `c1f4e1ab534715a83c7fa861b6a2997ed7abc8d46cf477681eab6dd3f4916eca` |
| GitHub asset | `state=uploaded`；`application/x-apple-diskimage` |
| GitHub size / digest | 23,828,770 bytes / `sha256:c1f4e1ab534715a83c7fa861b6a2997ed7abc8d46cf477681eab6dd3f4916eca` |
| 重新下载复核 | 与本地最终 DMG `cmp` 逐字节一致 |

初次 Tauri DMG 的外层 resource seal 不完整，因此没有上传。最终流程先对 Gateway、再对完成的外层 app 执行本地 ad-hoc seal，并从该 app 重新生成 DMG。

`hdiutil verify` 对最终 DMG 返回 `VALID`。只读挂载后的 app 版本为 0.8.0、bundle ID 为 `com.csswitch.menubar`，Desktop 与 Gateway 均为 Mach-O arm64；两者与构建目录中的已 seal 二进制逐字节一致。包内包含预期 icon 与 scripts，不含旧 `Contents/Resources/proxy`，DMG 的 Applications 链接指向 `/Applications`。

`codesign --verify --deep --strict --verbose=4` 对构建 app 和只读挂载 app 均通过；外层身份为 `Signature=adhoc`、`TeamIdentifier=not set`。这只证明 bundle seal 完整，不表示 Developer ID、notarization 或 Gatekeeper acceptance。

## README 与升级合同

- 根 README 中英文入口均指向 v0.8.0，并在首屏展示图标、产品摘要和 `docs/assets/csswitch-v0.8-ui-demo.gif`；
- CHANGELOG 记录新 UI、多模型角色、严格映射、Science 刷新和错误通知；
- 升级说明记录 schema v4 迁移，以及回到 v0.7.0 或更早版本前必须显式导出并降级到 v2 或恢复兼容备份。

## 未建立或不在本次声明中的层

- 从公开 v0.8.0 DMG 覆盖安装后执行的真实账号登录、provider 推理与 Science live E2E；
- 所有内置 provider、中转站、模型 ID、工具调用、Skill / MCP 和 SSH 环境的穷举验证；
- Developer ID 签名、Apple notarization、stapled ticket 与 Gatekeeper acceptance；
- macOS Intel、Windows、Linux 或 WSL 支持。

本次没有安装或替换 `/Applications/CSSwitch.app`，也没有读取或修改真实凭证与真实 Science data-dir。
